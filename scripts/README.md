# Scripts

Utility scripts for development and operations.

## Planned

- `dev-up.sh` — Start local development environment
- `test-all.sh` — Run all tests across all apps
- `lint-all.sh` — Lint all apps
- `import-report.sh` — Import a crawl report into the database (Phase 2+)

## Status

Phase 1: Placeholder. Scripts will be added as needed.
